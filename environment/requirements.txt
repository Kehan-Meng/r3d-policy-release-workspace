# Versions below mirror the currently validated Python 3.10 environment.
# CUDA-enabled Torch/PyTorch3D may need to be installed from platform-specific
# wheels before running this file on a new machine.
torch==2.4.1
torchvision==0.19.1
pytorch3d==0.7.8

# Configuration and training
hydra-core==1.2.0
omegaconf==2.3.0
diffusers==0.11.1
transformers==4.41.2
safetensors==0.7.0
accelerate>=0.30,<1.0
wandb==0.25.1
dill==0.3.5.1
termcolor==3.3.0
tqdm==4.67.3
PyYAML==6.0.3

# PointSAM and numerical stack
open-clip-torch==3.3.0
timm==1.0.26
einops==0.4.1
ftfy==6.3.1
regex>=2024.0
numpy==1.26.4
scipy==1.15.3
scikit-learn==1.7.2
pandas==2.3.3
zarr==2.12.0
numcodecs==0.13.1
h5py==3.16.0
open3d==0.18.0

# Rendering and benchmark runtimes. MetaWorld, ManiSkill2, and RoboTwin source
# checkouts are installed separately because the release uses benchmark-specific
# forks and must not be silently replaced by incompatible PyPI generations.
gym==0.21.0
gymnasium==0.29.1
mujoco==3.6.0
mujoco-py==2.1.2.14
sapien==3.0.0b1
opencv-python==4.11.0.86
imageio==2.34.2
Pillow==11.3.0
trimesh==4.4.3
transforms3d==0.4.2
mplib==0.2.1
toppra==0.6.6
natsort==8.4.0
matplotlib>=3.7
