# Heatmap Loss 配置
# BCEWithLogits + Dice + 256 维 CLS/EOT contrastive loss。

_target_: pc_sam.utils.loss.BCEDiceHeatmapLoss
bce_loss_weight: 1.0
dice_loss_weight: 1.0
dice_scale: 1000.0

# Uni3D-style symmetric InfoNCE. It is always enabled by train_change.py.
contrastive_loss_weight: 0.01
